# Neural Core Alpha-7 - SECURE Environment Template
# ⚠️  NEVER commit this file with real secrets!
# ⚠️  Copy to .env.local and fill with your actual credentials

# =============================================================================
# 🔒 SECURITY NOTICE
# =============================================================================
# This file contains sensitive credentials. 
# - Use strong, unique passwords
# - Never commit real secrets to version control
# - Rotate credentials regularly
# - Use a proper secret management system in production

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=1.0.0

# =============================================================================
# 🔐 AUTHENTICATION SECRETS (SERVER-SIDE ONLY)
# =============================================================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_super_secure_nextauth_secret_here_32_chars_min
NEXTAUTH_URL=http://localhost:3000

# JWT Secret (separate from NextAuth)
JWT_SECRET=your_jwt_secret_here_different_from_nextauth

# Demo password for development ONLY (REMOVE in production)
DEMO_PASSWORD=your_demo_password_here

# =============================================================================
# 🔐 TRADING API SECRETS (SERVER-SIDE ONLY)
# =============================================================================
# Moomoo API credentials - NEVER use NEXT_PUBLIC_ prefix for secrets!
MOOMOO_BASE_URL=http://127.0.0.1:11111
MOOMOO_API_KEY=your_moomoo_api_key_here
MOOMOO_SECRET_KEY=your_moomoo_secret_key_here
MOOMOO_ACCOUNT_ID=your_account_id_here

# Trading configuration
TRADING_MODE=paper
API_TIMEOUT=5000

# =============================================================================
# 🔐 DATABASE SECRETS (SERVER-SIDE ONLY)
# =============================================================================
# PostgreSQL Database URL with credentials
DATABASE_URL="postgresql://username:password@localhost:5432/neural_core_dev?schema=public"

# Redis credentials
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your_redis_password_here

# =============================================================================
# 🔐 AI ENGINE SECRETS (SERVER-SIDE ONLY)
# =============================================================================
# AI Engine authentication
AI_ENGINE_SECRET_KEY=your_ai_engine_secret_key_here
AI_ENGINE_JWT_SECRET=your_ai_engine_jwt_secret_here

# Qdrant Vector Database credentials
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=your_qdrant_api_key_here

# =============================================================================
# 🔐 EXTERNAL SERVICE SECRETS (SERVER-SIDE ONLY)
# =============================================================================
# Payment processing
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# Email service
SMTP_PASSWORD=your_smtp_password_here
SENDGRID_API_KEY=your_sendgrid_api_key_here

# Monitoring
SENTRY_DSN=your_sentry_dsn_here
SENTRY_AUTH_TOKEN=your_sentry_auth_token_here

# =============================================================================
# 🔐 ENCRYPTION KEYS (SERVER-SIDE ONLY)
# =============================================================================
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=your_256_bit_encryption_key_here_64_hex_chars
SESSION_SECRET=your_session_secret_here_32_chars_min

# =============================================================================
# ✅ PUBLIC CONFIGURATION (Safe for client-side)
# =============================================================================
# These are safe to expose to the browser
NEXT_PUBLIC_AI_ENGINE_URL=ws://localhost:3001
NEXT_PUBLIC_APP_NAME="Neural Core Alpha-7"
NEXT_PUBLIC_MAX_DEPOSIT=300
NEXT_PUBLIC_ENABLE_PAPER_TRADING=true
NEXT_PUBLIC_ENABLE_LIVE_TRADING=false

# Feature flags
NEXT_PUBLIC_ENABLE_AI_BRAIN_DASHBOARD=true
NEXT_PUBLIC_ENABLE_NEURAL_NETWORK_VIEW=true
NEXT_PUBLIC_ENABLE_TIMES_SQUARE_TICKERS=true

# Performance settings
NEXT_PUBLIC_AI_UPDATE_INTERVAL=2500
NEXT_PUBLIC_TICKER_SPEED=25000

# =============================================================================
# 🔒 SECURITY CHECKLIST
# =============================================================================
# Before going to production:
# □ Replace all placeholder values with real secrets
# □ Use a secret management service (AWS Secrets Manager, HashiCorp Vault, etc.)
# □ Enable SSL/TLS for all connections
# □ Rotate all secrets regularly
# □ Monitor for credential exposure in logs
# □ Use environment-specific configurations
# □ Implement proper access controls
# □ Enable audit logging
# □ Set up security monitoring alerts